/**
 * Tests for SeasonalGenerator
 *
 * This generator has no generator-specific logic beyond AIPromptGenerator base class behavior.
 * All meaningful behavior (prompt file selection, model tier, validation, generation) is
 * tested via base class integration tests.
 */

// TODO: Add generator-specific tests when SeasonalGenerator gains unique behavior
describe('SeasonalGenerator', () => {
  it.todo('has no generator-specific logic to test beyond base class behavior');
});
